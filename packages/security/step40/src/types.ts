export type ConsentId = string; export type ConsentVersion = `v${number}`; export type ConsentSubjectId = string; export type ConsentPurposeId = string; export type ConsentPolicyId = string; export enum ConsentStatus { GRANTED='GRANTED', DENIED='DENIED', WITHDRAWN='WITHDRAWN', EXPIRED='EXPIRED' } export enum ConsentAction { GRANT='GRANT', DENY='DENY', WITHDRAW='WITHDRAW', EXPIRE='EXPIRE' } export enum ConsentSource { WEB='WEB', APP='APP', PORTAL='PORTAL', API='API', ADMIN='ADMIN', SYSTEM='SYSTEM', EXTERNAL='EXTERNAL' } export enum ConsentScope { SUBJECT='SUBJECT', TENANT='TENANT', PURPOSE='PURPOSE', FEATURE='FEATURE', CUSTOM='CUSTOM' } export interface ConsentPurpose { id: ConsentPurposeId; name: string; description?: string; required: boolean; active: boolean; version: ConsentVersion; } export interface ConsentPolicy { id: ConsentPolicyId; version: ConsentVersion; name: string; purposes: ConsentPurposeId[]; active: boolean; effectiveAt: string; expiresAt?: string; } export interface ConsentRecord { id: ConsentId; subjectId: ConsentSubjectId; tenantId?: string; purposeId: ConsentPurposeId; status: ConsentStatus; action: ConsentAction; source: ConsentSource; scope: ConsentScope; policyId?: ConsentPolicyId; policyVersion?: ConsentVersion; capturedAt: string; expiresAt?: string; correlationId?: string; metadata?: Record<string, string | number | boolean>; } export interface ConsentContext { subjectId: ConsentSubjectId; tenantId?: string; regionCode?: string; locale?: string; } export interface ConsentCheckRequest { context: ConsentContext; purposeId: ConsentPurposeId; } export interface ConsentCheckResult { allowed: boolean; status: ConsentStatus; purposeId: ConsentPurposeId; recordId?: ConsentId; reason?: string; } export interface ConsentCaptureRequest { context: ConsentContext; purposeId: ConsentPurposeId; action: ConsentAction; source: ConsentSource; scope?: ConsentScope; policyId?: ConsentPolicyId; policyVersion?: ConsentVersion; expiresAt?: string; correlationId?: string; metadata?: Record<string, string | number | boolean>; }

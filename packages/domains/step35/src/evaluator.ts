import type { DecisionContext, RuleCondition, RuleDefinition, RuleGroup } from './types';
function getValue(context: DecisionContext, field: string): unknown { return field.split('.').reduce<unknown>((current, key) => { if (current && typeof current === 'object' && key in current) return (current as Record<string, unknown>)[key]; return undefined; }, context.attributes); }
function compare(condition: RuleCondition, actual: unknown): boolean { const expected = condition.value; switch (condition.operator) { case 'EQUALS': return actual === expected; case 'NOT_EQUALS': return actual !== expected; case 'GREATER_THAN': return typeof actual === 'number' && typeof expected === 'number' && actual > expected; case 'GREATER_THAN_OR_EQUAL': return typeof actual === 'number' && typeof expected === 'number' && actual >= expected; case 'LESS_THAN': return typeof actual === 'number' && typeof expected === 'number' && actual < expected; case 'LESS_THAN_OR_EQUAL': return typeof actual === 'number' && typeof expected === 'number' && actual <= expected; case 'IN': return Array.isArray(expected) && expected.includes(actual); case 'NOT_IN': return Array.isArray(expected) && !expected.includes(actual); case 'CONTAINS': return typeof actual === 'string' && typeof expected === 'string' && actual.includes(expected); case 'STARTS_WITH': return typeof actual === 'string' && typeof expected === 'string' && actual.startsWith(expected); case 'ENDS_WITH': return typeof actual === 'string' && typeof expected === 'string' && actual.endsWith(expected); case 'EXISTS': return actual !== undefined && actual !== null; case 'NOT_EXISTS': return actual === undefined || actual === null; } }
export function evaluateGroup(group: RuleGroup, context: DecisionContext): boolean { const direct = group.conditions.map(condition => compare(condition, getValue(context, condition.field))); const nested = (group.groups ?? []).map(child => evaluateGroup(child, context)); const values = [...direct, ...nested]; return group.operator === 'AND' ? values.every(Boolean) : values.some(Boolean); }
export function evaluateRule(rule: RuleDefinition, context: DecisionContext): boolean { return evaluateGroup(rule.condition, context); }

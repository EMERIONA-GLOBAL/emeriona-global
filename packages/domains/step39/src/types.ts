export type LocaleCode = string; export type TimeZone = string; export type CurrencyCode = string; export type RegionCode = string; export type FormatKind = 'DATE'|'TIME'|'DATETIME'|'NUMBER'|'CURRENCY'|'UNIT'|'PERCENT'|'CUSTOM'; export interface RegionalContext { locale: LocaleCode; timeZone: TimeZone; region?: RegionCode; currency?: CurrencyCode; } export interface FormatRequest { kind: FormatKind; value: string|number|Date; locale?: LocaleCode; timeZone?: TimeZone; currency?: CurrencyCode; unit?: string; options?: Record<string, unknown>; } export interface FormatResult { value: string; locale: LocaleCode; kind: FormatKind; } export interface RegionProfile { region: RegionCode; defaultLocale: LocaleCode; timeZone: TimeZone; currency: CurrencyCode; supportedLocales: LocaleCode[]; } export interface RegionalPolicy { id: string; version: string; enabled: boolean; allowedRegions?: RegionCode[]; blockedRegions?: RegionCode[]; defaultLocale: LocaleCode; fallbackLocale: LocaleCode; }

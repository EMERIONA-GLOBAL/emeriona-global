# Steps 47–64 migration queue

Source relocation is intentionally performed step-by-step into canonical owned layers. This marker prevents a false claim of full migration while the remaining source packages are being relocated and validated.

- 47–64: pending source relocation
- Step 38: pending source recovery
- No merge to `main` until migration and validation are complete.

import type { Product, ProductId, CatalogId } from "../../domains/src/catalog.js";
import type { ProductRepositoryPortV1 } from "../../domains/src/ports.js";
import type { UseCaseHandler, UseCaseRequest, UseCaseResponse } from "./index.js";
export interface CreateCatalogProductInput{readonly catalogId:string;readonly ownerId:string;readonly name:string;}
export class CreateCatalogProductHandler implements UseCaseHandler<CreateCatalogProductInput,Product>{constructor(private readonly repository:ProductRepositoryPortV1,private readonly id:()=>ProductId){}async handle(request:UseCaseRequest<CreateCatalogProductInput>):Promise<UseCaseResponse<Product>>{const catalogId=request.input.catalogId.trim(),ownerId=request.input.ownerId.trim(),name=request.input.name.trim();if(!catalogId||!ownerId||!name)throw new Error("catalogId, ownerId and name are required");const output=await this.repository.save({id:this.id(),catalogId:catalogId as CatalogId,ownerId:ownerId as Product["ownerId"],name,status:"DRAFT"});return{useCaseId:request.useCaseId,correlationId:request.context.correlationId,output};}}
export interface UpdateCatalogProductInput{readonly productId:string;readonly ownerId?:string;readonly catalogId?:string;readonly name?:string;readonly status?:Product["status"];}
export class UpdateCatalogProductHandler implements UseCaseHandler<UpdateCatalogProductInput,Product>{constructor(private readonly repository:ProductRepositoryPortV1){}async handle(request:UseCaseRequest<UpdateCatalogProductInput>):Promise<UseCaseResponse<Product>>{const productId=request.input.productId.trim();if(!productId)throw new Error("productId is required");const existing=await this.repository.findById(productId as ProductId);if(!existing)throw new Error("Product not found");const ownerId=request.input.ownerId===undefined?existing.ownerId:request.input.ownerId.trim();const catalogId=request.input.catalogId===undefined?existing.catalogId:(request.input.catalogId.trim()||undefined);const name=request.input.name===undefined?existing.name:request.input.name.trim();if(!ownerId||!name)throw new Error("ownerId and name cannot be empty");const output=await this.repository.save({...existing,ownerId:ownerId as Product["ownerId"],catalogId:catalogId as Product["catalogId"],name,status:request.input.status??existing.status});return{useCaseId:request.useCaseId,correlationId:request.context.correlationId,output};}}
export const CATALOG_PRODUCT_APPLICATION_VERSION="1.1.0" as const;

{
  "name": "@emeriona-global/web",
  "private": true,
  "version": "0.1.0",
  "description": "EMERIONA GLOBAL institutional web experience",
  "type": "module",
  "scripts": {
    "build": "node -e \"import('node:fs').then(({existsSync}) => { for (const file of ['index.html', 'styles.css']) { if (!existsSync(file)) throw new Error('Missing required web file: ' + file); } console.log('EMERIONA GLOBAL web build ready.'); })\""
  }
}
